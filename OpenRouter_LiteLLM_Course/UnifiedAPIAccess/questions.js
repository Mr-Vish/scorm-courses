test.AddQuestion( new Question ("q1",
                                "What is the primary benefit of using LiteLLM in a Python application?",
                                QUESTION_TYPE_CHOICE,
                                new Array("It generates images from text", "It provides a single, unified interface to call hundreds of different LLM providers using the OpenAI format", "It makes the LLM run locally on your GPU", "It acts as a database for storing chat history"),
                                "It provides a single, unified interface to call hundreds of different LLM providers using the OpenAI format",
                                "obj_module_1")
                );
