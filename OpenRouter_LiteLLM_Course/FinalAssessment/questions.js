// Final Comprehensive Assessment
// Covers all modules


test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q1",
                                "Regarding Unified API Access: What role does Usage alerts: Configure alerts when spending exceeds thresholds play in Unified API Access?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Provider arbitrage: Compare pricing across providers for the same model quality", "Random selection across deployments", "Usage alerts: Configure alerts when spending exceeds thresholds", "Even distribution"),
                                "Usage alerts: Configure alerts when spending exceeds thresholds",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.com.scorm.genaicourse.openrouterlitellm.final_q2",
                                "Regarding Unified API Access: What is a key characteristic of Provider arbitrage: Compare pricing across providers for the same model quality?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Pass-through + small markup", "Provider arbitrage: Compare pricing across providers for the same model quality", "Route to cheapest available deployment", "Latency-sensitive apps"),
                                "Provider arbitrage: Compare pricing across providers for the same model quality",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.com.scorm.genaicourse.openrouterlitellm.final_q3",
                                "Regarding Unified API Access: How is Open-source library + proxy best defined in this context?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Open-source library + proxy", "Pass-through + small markup", "100+ providers, bring your own keys", "Individual provider keys"),
                                "Open-source library + proxy",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q4",
                                "Regarding Unified API Access: In the context of Unified API Access, what does 200+ models, managed access refer to?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Routing Strategies", "Both together: Use LiteLLM as your abstraction layer with OpenRouter as one of the provide", "200+ models, managed access", "Pass-through + small markup"),
                                "200+ models, managed access",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q5",
                                "What is the primary benefit of the LiteLLM Proxy in an enterprise environment?",
                                QUESTION_TYPE_CHOICE,
                                new Array("It makes the models faster", "It provides a single, OpenAI-compatible endpoint with centralized key management, logging, and budgets", "It replaces the need for any AI models", "It is required by law in most countries"),
                                "It provides a single, OpenAI-compatible endpoint with centralized key management, logging, and budgets",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q6",
                                "How do 'Fallbacks' improve the reliability of AI applications?",
                                QUESTION_TYPE_CHOICE,
                                new Array("By automatically switching to a secondary model if the primary one fails or hits a rate limit", "By deleting any failed requests", "By making the prompts shorter", "By increasing the temperature of the model"),
                                "By automatically switching to a secondary model if the primary one fails or hits a rate limit",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q7",
                                "What is 'Model Aliasing'?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Giving the AI a human name", "Mapping a custom, stable model name (like 'smart-assistant') to a specific provider and model version", "Using a fake name when signing up for an API", "Translating the model's weights into a different language"),
                                "Mapping a custom, stable model name (like 'smart-assistant') to a specific provider and model version",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q8",
                                "Which caching technique uses vector embeddings to find similar prompts?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Exact-match Caching", "Semantic Caching", "Prompt Caching", "Browser Caching"),
                                "Semantic Caching",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q9",
                                "What does 'OpenRouter Ranking' use to order models?",
                                QUESTION_TYPE_CHOICE,
                                new Array("The alphabetical order of the model names", "Real-world usage data, latency, reliability, and cost-performance ratios", "The date the model was released", "Random selection"),
                                "Real-world usage data, latency, reliability, and cost-performance ratios",
                                "obj_final_assessment")
                );

test.AddQuestion( new Question ("com.scorm.openrouterlitellm.final_q10",
                                "Why are 'Virtual Keys' safer than master API keys?",
                                QUESTION_TYPE_CHOICE,
                                new Array("They are longer and harder to guess", "They can be restricted to specific models and budgets, and can be revoked individually without affecting other apps", "They are encrypted with a special AI algorithm", "They only work on Tuesdays"),
                                "They can be restricted to specific models and budgets, and can be revoked individually without affecting other apps",
                                "obj_final_assessment")
                );