test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q1",
                                "Which of the following best describes Use useState for accumulating streamed text?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Timeout: Set reasonable timeouts (30-60s) and show a retry button", "Use useState for accumulating streamed text", "SSE Implementation (Backend)", "Use useRef for auto-scrolling the chat container"),
                                "Use useState for accumulating streamed text",
                                "obj_module_1")
                );

test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q2",
                                "What role does Append each token to the state, triggering re-render play in Streaming Fundamentals?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Append each token to the state, triggering re-render", "Use useRef for auto-scrolling the chat container", "Simple streaming (most common for LLMs)", "Streaming Protocols"),
                                "Append each token to the state, triggering re-render",
                                "obj_module_1")
                );

test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q3",
                                "Which of the following is true regarding Use useRef for auto-scrolling the chat container?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Error Handling", "Server to client only", "SSE Implementation (Backend)", "Use useRef for auto-scrolling the chat container"),
                                "Use useRef for auto-scrolling the chat container",
                                "obj_module_1")
                );

test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q4",
                                "Which statement about Show a typing indicator during the stream is accurate?",
                                QUESTION_TYPE_CHOICE,
                                new Array("Error Handling", "Show a typing indicator during the stream", "The Latency Problem", "Simple streaming (most common for LLMs)"),
                                "Show a typing indicator during the stream",
                                "obj_module_1")
                );

test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q5",
                                "What is a key characteristic of Connection drops: Implement reconnection with the last received position?",
                                QUESTION_TYPE_CHOICE,
                                new Array("JavaScript SSE Client", "Connection drops: Implement reconnection with the last received position", "Timeout: Set reasonable timeouts (30-60s) and show a retry button", "SSE Implementation (Backend)"),
                                "Connection drops: Implement reconnection with the last received position",
                                "obj_module_1")
                );

test.AddQuestion( new Question ("com.scorm.streaming.genai.web.m1_q6",
                                "How is Timeout: Set reasonable timeouts (30-60s) and show a retry button best defined in this context?",
                                QUESTION_TYPE_CHOICE,
                                new Array("gRPC-based backends", "Timeout: Set reasonable timeouts (30-60s) and show a retry button", "Use useRef for auto-scrolling the chat container", "Error Handling"),
                                "Timeout: Set reasonable timeouts (30-60s) and show a retry button",
                                "obj_module_1")
                );